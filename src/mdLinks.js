import { resolve } from "url";
import { rejects } from "assert";

import { validRouteAbsolute } from './controller/path';

const mdLinks = (path, options) => {
    const promiseObj = new Promise((resolve, rejects) => {

    })
};